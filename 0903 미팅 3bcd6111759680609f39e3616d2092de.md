# 0903 미팅

## Dataset Analysis: ChatCFD (ConMoo/ChatCFD_dataset)

### 구조

- 총 205개 케이스 (자연어 설명 txt + polyMesh 메시 1:1 쌍)
- 입력: 자연어 설명 + constant/polyMesh/
- 에이전트 생성: 0/ (초기, 경계조건) + system/ (솔버 설정)
- 논문 재현 케이스: 4개 (NACA0012, 노즐 등)

### 카테고리 분포

- incompressible: 57개
- multiphase: 88개
- compressible: 30개
- heatTransfer: 14개 (전부 자연대류 buoyant 계열)
- combustion: 8개
- basic: 7개
- DNS: 1개

### 열유체 관점 분석

기존 14개 열전달 케이스가 전부 **자연대류**(buoyantFoam 계열)에 집중.
산업에서 자주 활용되는 **강제대류** 케이스가 부재함

- 강제대류 히트싱크 (핀 배열, 칩 냉각)
- 고체-유체 결합 (chtMultiRegionFoam, PCB/배터리)
- 채널 유동 + 가열 벽면 (열교환기)
- 강제 공랭 전자기기 (서버, GPU)

### 실행에 대한 검증

ChatCFD 논문에서 밝히는 실행 성공률은 82%, 물리적 타당성은 59%

→ "돌아가지만 물리적으로 틀린" 케이스라고 할 수 있음.
실제 구현 시에는 **물리적으로 타당한 결과인지**를 뒷받침하는게 중요함

---

### 연구 주제

**자율 CFD 에이전트를 통한 2D 열유체 형상 최적화
-** 자동 메싱, 에러 복구, 물리적 타당성 검증을 포함한 closed loop 설계 프레임워크

### 배경 및 문제의식

기존 히트싱크 최적화 연구는 대체로 균일 발열 조건에서 대칭형 핀 구조에 국한되어 있음.
따라서 실제 상황에서 주로 발생하는 hotspot이 존재하는 불균일 발열 환경에서
국소 부분의 냉각과 전체 압력손실 저감을 동시에 달성하는 비대칭 형상을 자율 탐색하는 프레임워크 제안

### 핵심 구현 방법

1. 입력 : 설계변수 3~5개 - 핀 높이, 간격, 곡률 제어점
2. 자동 메시 생성
3. CFD 실행 (2D, Re 100~500, 강제대류, Navier-Stokes + Energy Eq.)
    1. 메시/솔버 실패 시 자동 복구
    2. **결과의 물리적 타당성 검증**
    
    → 물리적 타당성 검증을 어떤걸 할건지, 그 방법은 어떻게 할건지
    
    1. 결과 : Q, P
4. 검증 통과한 결과를 모델에 반영, **베이지안 최적화(**Gaussian Process 기반)로 다음 시도할 형상 제안 → 반복

→ BO가 뭔지 설명

BO를 택한 이유: CFD처럼 한 번 돌리는 데 비용이 큰 경우 적은 횟수로 최적해 근처를 찾을 수 있음. 딥러닝에서처럼 대량 데이터 필요 없음.

### 실패 복구 설계

- **메시 생성 실패**: 지오메트리가 물리적으로 불가능한 경우(self-intersection 등) 메싱 시도 없이 즉시 페널티 부여 후 다음 설계로. 메시 품질 문제는 파라미터 완화하며 2~3회 재시도.
- **솔버 실행 실패**: 발산 시 시간 스텝 절반으로 줄여 1~2회 재시도. 미수렴 시 타임아웃 설정 후 실패로 기록, 다음 설계로 이동.
- 재시도 무한 반복 금지, 상한 도달 시 즉시 실패 처리 후 그 정보를 학습에 반영하고 넘어감.

### 물리적 타당성 검증

- **수치해석적 건전성** (매 케이스 자동): 질량 보존, 잔차 수렴 여부
- **물리 법칙 위반 여부** (매 케이스 자동): 에너지 보존(벽면 열유입 = 유체 엔탈피 증가), 비정상 압력 또는 온도 패턴 탐지
- **문헌 대비 검증** (baseline 1회): 원형/직사각형 핀 문헌값(Nu, ΔP)과 비교, 격자 수렴성 검증

Level 1/2는 파이프라인 안에 내장, 검증 실패 케이스는 이상치로 기록되어 모델 학습에서 제외함

### Config 정의

하나의 CFD 케이스를 재현하는 입력 명세 전체 (YAML/JSON): 형상 파라미터(BO 제안값), 경계조건(입구 유속, hotspot 위치, 출구 압력 등), 유동 조건(Re, 물성치), 솔버/메시 세팅. 결과는 Q, ΔP, 속도 및 온도장, 검증 통과 여부 메타데이터 등

BO 탐색 과정에서 축적되는 config→결과 쌍이 물리적 타당성이 검증된 열유체 데이터셋으로 자연스럽게 남게 되며, 별도 데이터셋 구축 작업 없이 최적화 과정을 통해 확보 가능

### 비교 및 산출물 (Performance Table)

전통적 대칭 핀 형상 vs BO 최적 형상을 비교
열전달량(Q), 압력손실(ΔP), PEC 지수 비교
최적 형상 주변 속도장, 온도장, streamline 시각화 및 물리적 해석 포함